import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('Teste básico de integridade do widget', (WidgetTester tester) async {
    // Apenas garante que o ambiente de testes do Flutter está operacional
    expect(true, isTrue);
  });
}